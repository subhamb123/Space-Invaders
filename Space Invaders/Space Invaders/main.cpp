// This example will use SFML libraries to implement
// the game of Pong

// History: 4/16 - Started from the SFML tutorial code that draws
//                a circle in the window.

//#include "Paddle.hpp"
//#include "Ball.hpp"
//
//int main()
//{
//	RenderWindow window(VideoMode(500, 500), "SFML works!");
//	Paddle p1Paddle(Vector2f(25.f, 50.f), Vector2f(0, 0), Color::Magenta);
//	Ball ball((float) p1Paddle.getSize().y / 10, Vector2f(window.getSize().x / 2, window.getSize().y / 2), Color::Cyan);
//
//	while (window.isOpen())
//	{
//		Event event;
//		while (window.pollEvent(event))
//		{
//			if (event.type == Event::Closed)
//				window.close();
//		}
//
//		window.clear();
//		window.draw(p1Paddle);
//		window.draw(ball);
//		window.display();
//	}
//...
//	return 0;
//}

#include "Virtualization.hpp"

int main() {
	Virtualization run;

	return 0;
}